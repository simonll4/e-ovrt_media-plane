# scripts/_archive

Scripts archivados el **2026-08-19** (pase de limpieza). Cero referencias en
`src/`, `tests/`, configs o docs (verificado).

- **`steelbench.py`** — script suelto de preparación del dataset SteelBench.
  Huérfano: ningún catálogo, test ni doc lo referencia. Era el único contenido de
  `scripts/datasets/`, que se eliminó al quedar vacío.
