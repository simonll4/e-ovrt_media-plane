# Smoke test del pipeline con detector mock (sin pesos) sobre CHV
run:
  scenario: DBE
  name: dbe_mock_chv
  description: "Smoke test (mock) leyendo CHV desde el repo hermano e-ovrt_datasets"
  max_units: 5

source:
  ref: chv

model:
  ref: mock

prompts:
  ref: cr01_cr02_v1
  active_ids: [person, helmet, vest]
