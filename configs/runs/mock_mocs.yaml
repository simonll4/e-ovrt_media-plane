# Smoke test del pipeline con detector mock (sin pesos) sobre MOCS (split test)
run:
  scenario: DBE
  name: dbe_mock_mocs
  description: "Smoke test (mock) leyendo MOCS test desde el repo hermano e-ovrt_datasets"
  max_units: 5

source:
  ref: mocs

model:
  ref: mock

prompts:
  ref: cr01_cr02_v1
  active_ids: [person, helmet, vest]
