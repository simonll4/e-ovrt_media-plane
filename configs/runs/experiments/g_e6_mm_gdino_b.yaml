# G-E6 — MM-Grounding-DINO base "all" en GPU, mismos prompts v1
run:
  scenario: DBE
  name: g_e6_mm_gdino_b
  description: "G-E6: MM-Grounding-DINO base (mezcla 'all') en GPU, prompts v1"

source:
  ref: dataset_v1

model:
  ref: mm-grounding-dino/mm-gdino-base
  device: cuda

prompts:
  ref: cr01_cr02_v1
  active_ids: [person, helmet, vest]

outputs:
  preview_max: 37
