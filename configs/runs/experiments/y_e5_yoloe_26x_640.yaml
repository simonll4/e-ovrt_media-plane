# Y-E5 — YOLOE-26x @ 640px en GPU. Techo de precisión local, si entra en VRAM (RTX 4060, 8 GB).
run:
  scenario: DBE
  name: y_e5_yoloe_26x_640
  description: "Y-E5: YOLOE-26x @ 640px en GPU, vocabulario corto v2 — techo de precisión local"

source:
  ref: dataset_v1

model:
  ref: yoloe/yoloe-26x
  device: cuda

prompts:
  ref: cr01_cr02_v2_short
  active_ids: [person, helmet, vest]

outputs:
  preview_max: 37
