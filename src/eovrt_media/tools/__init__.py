"""Utilitarios standalone del plano de medios (ex-CLI).

Cada módulo expone su función principal (mismo nombre/firma lógica que tenía
en `cli.py`) y un `main()` invocable como `python -m eovrt_media.tools.<name>`.
"""
